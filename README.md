signin-chromeapp
================

rewriting the sign-in app as a chrome app for offline use.

used yeoman (npm install -g yo)

dependencies handled with bower (npm install -g bower)
dependencies added via bower: jquery bootstrap
